html
    head
        link(ref="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons")
        link(rel="stylesheet" href="css/materialize.min.css" media="screen,projection")
        link(rel="stylesheet" href="css/app.css")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
    body
        script(type="text/javascript" src="js/materialize.min.js")
        div(class="container")
        - var post_id = 0
            each row in results
                if post_id == row['post_id']
                   div(class="row")
                       div(class="col 13")
                       div(class="postinfo")
                       div(class="row")
                           div(class="col s10 offset-s1 content")
                           div(class="col 12")
                              img(class="photo" src="photo/"+row['photo_id']+".png")
                else
                    div(class="row")
                        div(class="col 13")
                            img(class="icon" src="icons/"+row['account']+".png")
                        div(class="postinfo") #{row['name']}  Posts:#{row['post_count']} Followers:#{row['follower_count']} Following:#{row['followee_count']}
                        - var post_id = row['post_id']
                        div(class="row")
                            div(class="col 12")
                                img(class="photo" src="photo/"+row['photo_id']+".png")        
